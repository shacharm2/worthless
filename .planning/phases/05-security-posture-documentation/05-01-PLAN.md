---
phase: 05-security-posture-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_invariants.py
  - tests/test_security_properties.py
  - tests/test_security_posture.py
  - SECURITY.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "Invariant 3 (server-side direct upstream call) has an AST structural enforcement test matching the pattern of Invariants 1 and 2"
    - "SR-07 (constant-time compare) has an AST enforcement test proving hmac.compare_digest usage"
    - "SR-08 (CSPRNG only) has an AST enforcement test proving secrets module usage and random module absence"
    - "SECURITY.md exists at repo root with responsible disclosure policy"
    - "Smoke tests for SECURITY_POSTURE.md exist and skip gracefully when the doc does not yet exist"
  artifacts:
    - path: "tests/test_invariants.py"
      provides: "Extended with Invariant 3 structural AST enforcement test"
      contains: "test_.*invariant.*3\\|test_.*server_side\\|test_.*upstream"
    - path: "tests/test_security_properties.py"
      provides: "Extended with SR-07 and SR-08 AST enforcement tests"
      contains: "compare_digest\\|token_bytes"
    - path: "tests/test_security_posture.py"
      provides: "Smoke tests for SECURITY_POSTURE.md structure and completeness"
      min_lines: 40
    - path: "SECURITY.md"
      provides: "Vulnerability disclosure policy (GitHub Security tab)"
      min_lines: 20
  key_links:
    - from: "tests/test_invariants.py"
      to: "src/worthless/proxy/app.py"
      via: "AST scan for secure_key context manager usage"
      pattern: "secure_key"
    - from: "tests/test_security_properties.py"
      to: "src/worthless/crypto/splitter.py"
      via: "AST scan for hmac.compare_digest and secrets.token_bytes"
      pattern: "compare_digest|token_bytes"
---

<objective>
Close enforcement test gaps and create scaffolding for SECURITY_POSTURE.md. Before we can honestly document confidence levels, the enforcement tests must exist.

Purpose: The CONTEXT.md mandates writing structural tests before documenting claims. Invariant 3 and SR-07/SR-08 currently lack AST enforcement tests. This plan closes those gaps so Plan 02 can cite them as evidence.
Output: Extended test files with enforcement coverage, SECURITY.md disclosure policy, smoke test scaffold for the posture document.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-posture-documentation/05-CONTEXT.md
@.planning/phases/05-security-posture-documentation/05-RESEARCH.md
@SECURITY_RULES.md

Source files to scan for evidence and existing patterns:
@tests/test_invariants.py
@tests/test_security_properties.py
@src/worthless/crypto/splitter.py
@src/worthless/crypto/types.py
@src/worthless/proxy/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close enforcement test gaps (Invariant 3, SR-07, SR-08)</name>
  <files>tests/test_invariants.py, tests/test_security_properties.py</files>
  <behavior>
    - Invariant 3 structural test: AST-scan proxy/app.py to confirm reconstructed key material is used inside a `secure_key` context manager (or equivalent zeroing pattern) and never assigned to a variable that escapes the with-block scope. Follow the existing AST-scan pattern used for Invariants 1 and 2 in test_invariants.py.
    - SR-07 test: AST-scan all files in src/worthless/ that import `hmac` — confirm they use `hmac.compare_digest` and never use `==` or `!=` to compare digest/hash values.
    - SR-08 test: AST-scan all files in src/worthless/ — confirm `secrets.token_bytes` (or `secrets.token_hex`, `secrets.token_urlsafe`) is used for randomness, and `random.` module is never imported (reinforces existing Ruff TID251 rule with a test-level check).
  </behavior>
  <action>
Read the existing test_invariants.py to understand the AST-scan pattern (likely uses `ast.parse` + `ast.walk` to find imports and function calls). Then:

1. **Invariant 3 test** (add to tests/test_invariants.py): Write a test that AST-parses `src/worthless/proxy/app.py` and verifies that `decrypt_shard` or shard reconstruction result is consumed inside a `secure_key` context manager. The test should confirm the key material flows through the context manager (proving server-side containment + zeroing). Name it clearly: `test_invariant3_reconstructed_key_contained_in_secure_key` or similar.

2. **SR-07 test** (add to tests/test_security_properties.py): Write a test that finds all `.py` files in `src/worthless/` that contain `hmac` usage, then AST-parses them to confirm `hmac.compare_digest` is the only comparison function used (no `==` on digest results). Name: `test_sr07_constant_time_compare_enforced`.

3. **SR-08 test** (add to tests/test_security_properties.py): Write a test that scans all `.py` files in `src/worthless/` for imports of the `random` module (not `secrets`). Confirm `random` is never imported. Also confirm that files generating random bytes use `secrets.token_bytes`. Name: `test_sr08_csprng_only_enforced`.

Follow the existing code style and test organization patterns. Use `ast` module for parsing, `pathlib` for file discovery. Each test should have a clear docstring explaining what security rule it enforces.
  </action>
  <verify>
    <automated>cd /Users/shachar/Projects/worthless/worthless && uv run pytest tests/test_invariants.py tests/test_security_properties.py -v -x 2>&1 | tail -30</automated>
  </verify>
  <done>Three new AST enforcement tests pass: Invariant 3 structural test, SR-07 constant-time compare, SR-08 CSPRNG-only. All existing tests in both files still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create SECURITY.md and SECURITY_POSTURE.md smoke test</name>
  <files>SECURITY.md, tests/test_security_posture.py</files>
  <action>
**SECURITY.md** — Create at repo root. This is the GitHub-standard vulnerability disclosure policy that surfaces in the repo's Security tab. Contents:

1. **Security Policy header**
2. **Supported Versions** table: v0.x (Python PoC) = supported for security fixes
3. **Reporting a Vulnerability**: responsible disclosure process
   - Contact: security email or GitHub private vulnerability reporting
   - Response SLA: 48-hour acknowledgment, 7-day triage, 90-day disclosure timeline
   - Scope: crypto, proxy, storage, CLI — anything that could weaken key protection
   - Out of scope: denial of service on the proxy (it's self-hosted), social engineering
4. **Safe Harbor**: standard safe harbor statement for good-faith security researchers
5. **Preferred Languages**: English
6. Link to SECURITY_POSTURE.md for full threat model and known limitations

Keep it concise — this is a standard policy doc, not the posture assessment.

**tests/test_security_posture.py** — Create smoke test file that validates SECURITY_POSTURE.md structure. All tests MUST use `@pytest.mark.skipif(not Path("SECURITY_POSTURE.md").exists(), reason="SECURITY_POSTURE.md not yet created")` so they skip gracefully until Plan 02 creates the document.

Test functions (each a separate test for clear failure messages):
1. `test_security_posture_exists` — file exists at repo root
2. `test_has_table_of_contents` — contains "Table of Contents" or "## Contents"
3. `test_has_glossary` — contains "Glossary" section
4. `test_covers_all_three_invariants` — contains "Client-Side Splitting", "Gate Before Reconstruction", "Server-Side Direct Upstream Call" (or close variants)
5. `test_covers_all_security_rules` — SR-01 through SR-08 all appear
6. `test_uses_defined_confidence_scale` — contains "Enforced", "Best-effort", "Planned" (the 3-tier scale from CONTEXT.md)
7. `test_has_known_limitations` — contains "Known Limitation" or "Limitations"
8. `test_has_rust_mitigation` — "Rust" or "zeroize" appears
9. `test_has_non_goals` — contains "Non-Goals" or "Out of Scope" or "Does NOT protect"
10. `test_cites_test_evidence` — contains "test_invariants" and "test_security_properties"
11. `test_has_trust_boundary_diagram` — contains diagram markers (ASCII art or "Trust Boundary")
12. `test_has_residual_risk_table` — contains "Residual Risk"
13. `test_has_changelog` — contains "Changelog"
14. `test_has_last_verified_date` — contains "Last verified:" or "Last reviewed:"
15. `test_links_to_security_md` — contains "SECURITY.md"
16. `test_no_compliance_overclaiming` — does NOT contain "SOC 2 certified" or "FIPS validated" or "ISO 27001 certified"

Also add a test for SECURITY.md:
17. `test_security_md_exists` — SECURITY.md exists at repo root (no skip — it's created in this task)
  </action>
  <verify>
    <automated>cd /Users/shachar/Projects/worthless/worthless && uv run pytest tests/test_security_posture.py -v 2>&1 | tail -25</automated>
  </verify>
  <done>SECURITY.md exists at repo root with disclosure policy. tests/test_security_posture.py has 17 test functions — 16 skip gracefully (SECURITY_POSTURE.md not yet created), 1 passes (SECURITY.md exists). Full test suite green.</done>
</task>

</tasks>

<verification>
1. `uv run pytest tests/test_invariants.py tests/test_security_properties.py -v -x` — all enforcement tests pass including new ones
2. `uv run pytest tests/test_security_posture.py -v` — smoke tests skip gracefully (except SECURITY.md test which passes)
3. `test -f SECURITY.md` — disclosure policy exists
4. `uv run pytest tests/ -x -q` — full suite green, no regressions
</verification>

<success_criteria>
- Invariant 3 has AST structural enforcement test (matching Inv 1/2 pattern)
- SR-07 and SR-08 have AST enforcement tests
- SECURITY.md exists with responsible disclosure policy
- Smoke test scaffold for SECURITY_POSTURE.md ready (skips gracefully)
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/05-security-posture-documentation/05-01-SUMMARY.md`
</output>
