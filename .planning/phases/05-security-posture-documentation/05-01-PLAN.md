---
phase: 05-security-posture-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY_POSTURE.md
  - tests/test_security_posture.py
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "SECURITY_POSTURE.md exists at repo root with protection status for each of the 3 architectural invariants"
    - "Each invariant has a confidence level from the defined 4-tier scale (HIGH/MEDIUM/LOW/PLANNED)"
    - "Python PoC memory limitations (GC non-determinism, no mlock, intermediate bytes) are explicitly documented"
    - "Each limitation has a planned Rust mitigation path"
    - "All 8 security rules (SR-01 through SR-08) have compliance status and evidence"
    - "Non-goals section explicitly states what Worthless does NOT protect against"
  artifacts:
    - path: "tests/test_security_posture.py"
      provides: "Smoke test validating document structure and completeness"
      min_lines: 30
    - path: "SECURITY_POSTURE.md"
      provides: "Security posture self-assessment document"
      min_lines: 150
      contains: "Invariant"
  key_links:
    - from: "SECURITY_POSTURE.md"
      to: "SECURITY_RULES.md"
      via: "Cross-references SR-01 through SR-08"
      pattern: "SR-0[1-8]"
    - from: "SECURITY_POSTURE.md"
      to: "tests/test_invariants.py"
      via: "Evidence citations for invariant enforcement"
      pattern: "test_invariants"
    - from: "SECURITY_POSTURE.md"
      to: "tests/test_security_properties.py"
      via: "Evidence citations for crypto property tests"
      pattern: "test_security_properties"
---

<objective>
Create an honest, auditable SECURITY_POSTURE.md that states exactly what Worthless protects, at what confidence level, and what its known limitations are.

Purpose: Give users, auditors, and contributors a transparent self-assessment of the Python PoC's security guarantees -- no overclaiming, no false modesty.
Output: SECURITY_POSTURE.md at repo root + smoke test validating document structure.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-security-posture-documentation/05-RESEARCH.md
@SECURITY_RULES.md

Source files for evidence gathering:
@tests/test_invariants.py
@tests/test_security_properties.py
@src/worthless/crypto/splitter.py
@src/worthless/crypto/types.py
@src/worthless/proxy/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create validation smoke test for SECURITY_POSTURE.md</name>
  <files>tests/test_security_posture.py</files>
  <action>
Create a pytest smoke test file that validates the structure and completeness of SECURITY_POSTURE.md. The test should:

1. Assert SECURITY_POSTURE.md exists at repo root
2. Assert all 3 architectural invariants are covered (search for "Client-Side Splitting", "Gate Before Reconstruction", "Server-Side Direct Upstream Call")
3. Assert all 8 security rules are referenced (SR-01 through SR-08)
4. Assert confidence levels use only the defined scale: HIGH, MEDIUM, LOW, PLANNED
5. Assert a "Known Limitations" section exists
6. Assert "Rust" appears in the document (hardening roadmap)
7. Assert a "Non-Goals" or "Out of Scope" section exists
8. Assert test evidence files are cited (test_invariants, test_security_properties)

Use simple file read + string/regex checks. No complex parsing needed. Each check should be a separate test function for clear failure messages. Use `pytest.importorskip` pattern or `skipif` if SECURITY_POSTURE.md doesn't exist yet (so the test file can be committed before the doc).

Mark all tests with `@pytest.mark.skipif(not Path("SECURITY_POSTURE.md").exists(), reason="SECURITY_POSTURE.md not yet created")` so they skip gracefully until Task 2 completes.
  </action>
  <verify>
    <automated>cd /Users/shachar/Projects/worthless/worthless && uv run pytest tests/test_security_posture.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>tests/test_security_posture.py exists with 8+ test functions. All tests skip cleanly (SECURITY_POSTURE.md not yet created).</done>
</task>

<task type="auto">
  <name>Task 2: Write SECURITY_POSTURE.md</name>
  <files>SECURITY_POSTURE.md</files>
  <action>
Create SECURITY_POSTURE.md at repo root following the structure from 05-RESEARCH.md. The document MUST contain these sections in order:

**1. Purpose and Scope**
- State this is a self-assessment, NOT a certification claim
- Scope: Python PoC (v0.x), not the planned Rust hardening
- Audience: developers using Worthless, security reviewers, contributors
- Last reviewed date: today's date

**2. Threat Model**
- What we defend against: stolen API keys (exfiltrated .env, leaked in logs, committed to git)
- What we do NOT defend against (NON-GOALS): memory forensics on a compromised host, side-channel attacks, supply chain attacks on dependencies, compromised proxy server, nation-state adversaries with physical access
- Attacker model: external attacker with read access to developer's filesystem or network traffic

**3. Architectural Invariants**
For each of the 3 invariants, create an assessment card (table) with:
- Status (ENFORCED/PARTIAL/PLANNED)
- Confidence level (HIGH/MEDIUM/LOW/PLANNED)
- Enforcement mechanism
- Test evidence (cite specific test file::class or test file::function names by reading the actual test files)
- Known gaps
- Narrative explanation
- Hardening path

Invariant details:
- **Client-Side Splitting**: Status ENFORCED, Confidence HIGH. Evidence from test_invariants.py AST scan tests. Gap: dynamic imports not caught. Rust fix: split_key won't exist in reconstruction binary.
- **Gate Before Reconstruction**: Status ENFORCED, Confidence HIGH. Evidence from test_security_properties.py gate-before-decrypt tests + proxy/app.py source order. Rust fix: process boundary enforcement.
- **Server-Side Direct Upstream Call**: Status ENFORCED, Confidence HIGH. Evidence from test_invariants.py secure_key usage test. Rust fix: distroless container, key never leaves reconstruction process memory.

IMPORTANT: Read the actual test files to get exact test class/function names. Do NOT use placeholder names from the research doc without verifying.

**4. Security Rules Compliance**
Table with columns: Rule | Description | Status | Confidence | Evidence
Fill in for SR-01 through SR-08 using SECURITY_RULES.md as source. Be honest about confidence:
- SR-01 (bytearray): HIGH
- SR-02 (zeroing): MEDIUM (GC limitation)
- SR-03 (gate ordering): HIGH
- SR-04 (no telemetry): HIGH
- SR-05 (logging denylist): MEDIUM (gitleaks enforced, runtime audit incomplete)
- SR-06 (sidecar isolation): LOW (in-process in Python PoC)
- SR-07 (constant-time): HIGH
- SR-08 (CSPRNG): HIGH

**5. Known Limitations (Python PoC)**
Two subsections:
- Memory Safety: GC non-determinism, no mlock, intermediate bytes objects, no compiler barrier. Use the table format from research.
- Process Isolation: in-process reconstruction, shared memory space. Use the table format from research.

Each limitation gets: what it is, impact, current mitigation, Rust fix.

**6. Hardening Roadmap**
Brief section mapping PLANNED/LOW confidence items to specific Rust mechanisms:
- zeroize crate for memory zeroing
- mlock() for swap protection
- distroless container for process isolation
- seccomp for syscall filtering
- Process boundary between gate and reconstruction

**7. Test Evidence Summary**
Table mapping security claims to test files with approximate test counts. Reference:
- tests/test_invariants.py
- tests/test_security_properties.py
- tests/test_splitter.py (crypto primitives)
- tests/test_security_posture.py (meta: this document's structure)

**8. Changelog**
Initial entry with today's date: "Initial security posture assessment for Python PoC v0.x"

**Style rules:**
- No FIPS/SOC2 compliance language
- Every claim cites evidence (test file, code path, or security rule)
- "Python PoC" qualifier on all guarantees
- Honest about limitations -- this is a PoC, not production-hardened
- Reference SECURITY_RULES.md as the living enforcement document
  </action>
  <verify>
    <automated>cd /Users/shachar/Projects/worthless/worthless && uv run pytest tests/test_security_posture.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>SECURITY_POSTURE.md exists at repo root. All smoke tests in test_security_posture.py pass. Document covers all 3 invariants with confidence levels, all 8 SR rules, known limitations with Rust mitigation paths, and non-goals section.</done>
</task>

</tasks>

<verification>
1. `test -f SECURITY_POSTURE.md` -- file exists at repo root
2. `uv run pytest tests/test_security_posture.py -v` -- all structure/completeness smoke tests pass
3. `uv run pytest tests/ -x -q` -- full test suite still green (no regressions)
4. `grep -c "SR-0" SECURITY_POSTURE.md` -- returns 8+ (all security rules referenced)
5. `grep -c "Invariant" SECURITY_POSTURE.md` -- returns 3+ (all invariants covered)
</verification>

<success_criteria>
- SECURITY_POSTURE.md exists with all 8 sections
- All 3 architectural invariants assessed with confidence levels from the 4-tier scale
- All 8 security rules have compliance status and evidence
- Python PoC limitations (memory model, GC, no mlock) explicitly documented
- Each limitation has a Rust hardening path
- Non-goals section states what Worthless does NOT protect against
- Smoke tests validate document structure automatically
- Full test suite remains green
</success_criteria>

<output>
After completion, create `.planning/phases/05-security-posture-documentation/05-01-SUMMARY.md`
</output>
