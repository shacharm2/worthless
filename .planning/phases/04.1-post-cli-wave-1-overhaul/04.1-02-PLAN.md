---
phase: 04.1-post-cli-wave-1-overhaul
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - README.md
  - docs/PROTOCOL.md
  - docs/install-mcp.md
  - docs/install-openclaw.md
  - docs/install-solo.md
  - docs/install-self-hosted.md
  - docs/install-teams.md
  - docs/install-github-actions.md
  - examples/mcp.json
  - examples/ci/worthless-ci.yml
autonomous: false
requirements:
  - 04.1b-README
  - 04.1b-DOCS
  - 04.1b-EXAMPLES
  - 04.1b-PRECOMMIT
  - 04.1b-WALKTHROUGH

must_haves:
  truths:
    - "README quickstart shows real terminal output from lock, wrap, and status commands"
    - "README uses lock/unlock terminology, never enroll in user-facing sections"
    - "All integration docs reference port 8787 and have Planned banners for cloud sections"
    - "docs/PROTOCOL.md exists with headers, endpoints, error codes, and security model"
    - "examples/ directory contains mcp.json and ci/worthless-ci.yml"
    - "README is under 200 lines and uses plain markdown only"
  artifacts:
    - path: "README.md"
      provides: "Quickstart-first README with real terminal output"
    - path: "docs/PROTOCOL.md"
      provides: "Wire protocol reference"
    - path: "docs/install-mcp.md"
      provides: "Local-first MCP integration doc"
    - path: "docs/install-github-actions.md"
      provides: "GitHub Actions CI workflow doc"
    - path: "examples/mcp.json"
      provides: "Copy-paste MCP config"
    - path: "examples/ci/worthless-ci.yml"
      provides: "Copy-paste GitHub Actions workflow"
  key_links:
    - from: "README.md"
      to: "docs/PROTOCOL.md"
      via: "markdown link"
      pattern: "PROTOCOL\\.md"
    - from: "README.md"
      to: "SECURITY_RULES.md"
      via: "markdown link"
      pattern: "SECURITY_RULES\\.md"
    - from: "docs/install-mcp.md"
      to: "examples/mcp.json"
      via: "reference to example file"
      pattern: "examples/mcp\\.json"
---

<objective>
Rewrite README and all integration docs to tell the same story the shipped CLI tells. Quickstart-first, real terminal output, local-first integration docs with Planned banners.

Purpose: The README is the product's front door. It must honestly reflect what works today (local CLI) and clearly mark what is planned. This is the main deliverable of Phase 04.1.
Output: README.md (~1000 words, <200 lines), PROTOCOL.md, rewritten install docs, examples/ directory.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-CONTEXT.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-RESEARCH.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-01-SUMMARY.md
@.planning/research/04.1-readme-launch/FINAL-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README rewrite + PROTOCOL.md + examples</name>
  <files>
    README.md,
    docs/PROTOCOL.md,
    examples/mcp.json,
    examples/ci/worthless-ci.yml
  </files>
  <action>
1. **Manual quickstart walkthrough (04.1b-WALKTHROUGH):**
   - From a clean state, run the actual quickstart commands and capture real terminal output:
     - `worthless lock` (with a test API key)
     - `worthless wrap python -c "import os; print(os.environ.get('OPENAI_BASE_URL', 'not set'))"`
     - `worthless status`
     - `worthless unlock` + `worthless down`
   - Save the real output verbatim for inclusion in README. Do NOT fabricate or edit output.

2. **README rewrite (04.1b-README, ~1000 words, <200 lines):**
   Follow this exact section order from CONTEXT.md:
   1. Title + "Make your API keys worthless to steal" tagline + 4 badges (Python 3.10+, AGPL-3.0, pre-release, tests — use shields.io static badges)
   2. Three-line value prop: split, proxy, budget-blown-means-key-never-forms
   3. Pre-release callout using GitHub `[!NOTE]` admonition
   4. Quickstart using REAL command output from step 1. Wrap-first progressive disclosure: `git clone` -> `uv pip install -e .` -> `worthless lock` -> `worthless wrap python app.py`
   5. "What just happened" — inline explanation of equivalent manual steps (worthless up + BASE_URL). User reads, doesn't run.
   6. Show `worthless status` output as trust anchor
   7. "Undo everything" — `worthless unlock` + `worthless down`
   8. How it works — ASCII diagram of split/proxy/reconstruct flow (Claude's discretion on style)
   9. CLI reference table — all commands, one-line descriptions
   10. Positioning — 2 sentences only: "Every secrets tool protects the key until your app gets it. Worthless protects you after it leaks." NO comparison table.
   11. "What Worthless does NOT protect against" — explicit non-goals (memory inspection on compromised host, supply chain attacks, keys already leaked before enrollment)
   12. Security section — link SECURITY_RULES.md, name crypto primitives (XOR, HMAC-SHA256), "no novel cryptography", honest no-audit-yet. NO Rust mention.
   13. Pre-commit hook section (~5 lines showing .pre-commit-config.yaml setup with worthless-scan)
   14. Minimal Dev + Contributing footer — `uv sync`, `pytest`, `ruff check`. Link SECURITY_RULES.md for crypto work.

   **Do NOT include:** Install links to planned features, comparison table, centered HTML, `--json` claim beyond status/scan, fabricated output, Rust hardening, OpenSSF badge.
   **Terminology:** `lock`/`unlock` everywhere user-facing. Never "enroll" in README.

3. **PROTOCOL.md (04.1b-DOCS partial):**
   Create `docs/PROTOCOL.md` — slim wire protocol doc:
   - Pre-release banner at top
   - Headers table: `x-worthless-key` (identifies enrolled key), `x-worthless-shard-a` (client shard for reconstruction)
   - Proxy endpoints table: `/v1/chat/completions` (OpenAI), `/v1/messages` (Anthropic)
   - Error codes: list WRTLS-NNN codes from the codebase
   - Environment variables: WORTHLESS_PORT, WORTHLESS_DB_PATH, etc.
   - Half-page Security Model: shard custody (A stays client-side), gate-before-reconstruct, server-side direct call
   - Do NOT include: XOR mechanics, KMS internals, memory zeroing details, rules engine internals

4. **Examples directory (04.1b-EXAMPLES):**
   - Create `examples/mcp.json` — copy-paste MCP config pointing at localhost:8787 for Claude Code / Cursor / Windsurf
   - Create `examples/ci/worthless-ci.yml` — copy-paste GitHub Actions workflow showing local proxy in CI (YAML syntax valid, functional testing deferred to 04.2)

**Note on header name:** Use `x-worthless-key` in all new docs (forward-looking). The actual source code rename happens in Plan 03. README and PROTOCOL.md should use the new name since they are being written fresh.
  </action>
  <verify>
    <automated>wc -l README.md && test -f docs/PROTOCOL.md && test -f examples/mcp.json && test -f examples/ci/worthless-ci.yml && echo "All files exist" || echo "MISSING FILES"</automated>
  </verify>
  <done>README under 200 lines with real terminal output. PROTOCOL.md exists with wire protocol reference. examples/ directory populated.</done>
</task>

<task type="auto">
  <name>Task 2: Integration docs rewrite</name>
  <files>
    docs/install-mcp.md,
    docs/install-openclaw.md,
    docs/install-solo.md,
    docs/install-self-hosted.md,
    docs/install-teams.md,
    docs/install-github-actions.md
  </files>
  <action>
1. **install-mcp.md — Rewrite local-first:**
   - Show `.mcp.json` pointing at `localhost:8787`
   - Cover Claude Code, Cursor, and Windsurf setup (each has slightly different config location)
   - Reference `examples/mcp.json` as copy-paste source
   - Cloud MCP server section gets `[!NOTE] Planned` banner
   - Minimal — just enough for agent discovery with local proxy

2. **install-openclaw.md — Planned banner only:**
   - `[!NOTE] Planned` banner at top
   - One-liner: "OpenClaw integration ships with MCP server."
   - Cut any existing ClawHub skill reference

3. **install-solo.md — Target-state doc:**
   - Rewrite as what it will look like when PyPI + domain are live
   - `[!NOTE]` banner: "Not yet available. Install from source — see README."
   - Port references: 8787

4. **install-self-hosted.md + install-teams.md — Keep with banners:**
   - Add `[!NOTE] Planned` banner to each
   - Ensure port references are 8787
   - Consistent treatment with other planned docs

5. **install-github-actions.md — NEW file:**
   - Real GitHub Actions workflow YAML (reference examples/ci/worthless-ci.yml)
   - Show: install worthless from source, run `worthless lock`, run `worthless wrap pytest`
   - `[!NOTE]` banner for sections about hosted proxy in CI
   - Actual CI gate testing deferred to Phase 04.2 (WOR-7)

6. **Terminology check:** grep all docs/ files for "enroll" — only allowed in PROTOCOL.md and architecture/security docs. User-facing integration docs must use lock/unlock.
  </action>
  <verify>
    <automated>ls docs/install-mcp.md docs/install-openclaw.md docs/install-solo.md docs/install-self-hosted.md docs/install-teams.md docs/install-github-actions.md docs/PROTOCOL.md && grep -l 'Planned' docs/install-openclaw.md docs/install-self-hosted.md docs/install-teams.md && echo "OK" || echo "MISSING"</automated>
  </verify>
  <done>All 6 install docs exist. OpenClaw, self-hosted, and teams have Planned banners. MCP doc is local-first. GitHub Actions doc has real workflow YAML. No "enroll" in user-facing docs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify README and docs</name>
  <action>Human reviews all documentation output from Tasks 1 and 2.</action>
  <what-built>Complete README rewrite with real terminal output, PROTOCOL.md wire protocol reference, 6 integration docs (local-first with Planned banners), and examples/ directory.</what-built>
  <how-to-verify>
    1. Open README.md — verify quickstart shows real command output, uses lock/unlock (never enroll), under 200 lines, plain markdown, no centered HTML
    2. Follow the quickstart steps from a clean state — do they actually work?
    3. Open docs/PROTOCOL.md — verify headers, endpoints, error codes, security model section
    4. Open docs/install-mcp.md — verify local-first with localhost:8787
    5. Spot-check install-openclaw.md and install-teams.md for Planned banners
    6. Check examples/mcp.json is valid JSON pointing at 8787
    7. Verify no mention of Rust, PyPI install, curl worthless.sh, or comparison table in README
  </how-to-verify>
  <verify>Human approval</verify>
  <done>User confirms README and docs are accurate, honest, and ready for public.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `wc -l README.md` — under 200 lines
2. `grep -i 'enroll' README.md` — zero results (only lock/unlock in user-facing docs)
3. `grep -rn '8443\|9191' docs/ README.md examples/` — zero results
4. All files exist: PROTOCOL.md, install-github-actions.md, examples/mcp.json, examples/ci/worthless-ci.yml
5. Manual: quickstart walkthrough succeeds end-to-end
</verification>

<success_criteria>
- README is quickstart-first, under 200 lines, with real terminal output
- PROTOCOL.md covers wire protocol (headers, endpoints, errors, security model)
- All integration docs are local-first with Planned banners for cloud features
- examples/ directory has mcp.json and ci workflow
- No fabricated output, no Rust mention, no planned features presented as current
- User approves via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-02-SUMMARY.md`
</output>
