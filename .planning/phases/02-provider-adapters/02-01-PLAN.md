---
phase: 02-provider-adapters
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/worthless/adapters/__init__.py
  - src/worthless/adapters/types.py
  - src/worthless/adapters/openai.py
  - src/worthless/adapters/anthropic.py
  - src/worthless/adapters/registry.py
  - tests/test_adapters.py
  - tests/conftest.py
autonomous: true
requirements:
  - PROX-01
  - PROX-02

must_haves:
  truths:
    - "An OpenAI-format request is transformed into a valid upstream request with correct URL and Bearer auth header"
    - "An Anthropic-format request is transformed into a valid upstream request with correct URL, x-api-key header, and anthropic-version header"
    - "OpenAI non-streaming response is relayed with original status code and body unchanged"
    - "Anthropic non-streaming response is relayed with original status code and body unchanged"
    - "Unrecognized provider paths return 404"
  artifacts:
    - path: "src/worthless/adapters/types.py"
      provides: "AdapterRequest and AdapterResponse dataclasses, ProviderAdapter Protocol"
      exports: ["AdapterRequest", "AdapterResponse", "ProviderAdapter"]
    - path: "src/worthless/adapters/openai.py"
      provides: "OpenAI adapter implementing request transform and response relay"
      exports: ["OpenAIAdapter"]
    - path: "src/worthless/adapters/anthropic.py"
      provides: "Anthropic adapter implementing request transform and response relay"
      exports: ["AnthropicAdapter"]
    - path: "src/worthless/adapters/registry.py"
      provides: "Path-based adapter lookup"
      exports: ["get_adapter"]
    - path: "tests/test_adapters.py"
      provides: "Unit tests for PROX-01 and PROX-02"
      min_lines: 80
  key_links:
    - from: "src/worthless/adapters/openai.py"
      to: "src/worthless/adapters/types.py"
      via: "implements ProviderAdapter protocol"
      pattern: "class OpenAIAdapter"
    - from: "src/worthless/adapters/anthropic.py"
      to: "src/worthless/adapters/types.py"
      via: "implements ProviderAdapter protocol"
      pattern: "class AnthropicAdapter"
    - from: "src/worthless/adapters/registry.py"
      to: "src/worthless/adapters/openai.py"
      via: "maps path to adapter instance"
      pattern: "get_adapter"
---

<objective>
Define adapter contracts and implement stateless request/response transformers for OpenAI and Anthropic non-streaming requests.

Purpose: Establish the adapter interface that Phase 3 (Proxy Service) will consume, and prove that request transformation and response relay work correctly for both providers before adding streaming complexity.

Output: `src/worthless/adapters/` package with types, OpenAI adapter, Anthropic adapter, registry, and passing unit tests.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-provider-adapters/02-CONTEXT.md
@.planning/phases/02-provider-adapters/02-RESEARCH.md
@.planning/phases/02-provider-adapters/02-VALIDATION.md

# Phase 1 summary for project conventions (TDD pattern, package structure)
@.planning/phases/01-crypto-core-and-storage/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define adapter contracts and write failing tests</name>
  <files>src/worthless/adapters/__init__.py, src/worthless/adapters/types.py, tests/test_adapters.py, tests/conftest.py</files>
  <behavior>
    - test_openai_request_transform: Given a request body and API key, OpenAI adapter produces upstream URL "https://api.openai.com/v1/chat/completions", sets "Authorization: Bearer {key}" header, forwards body unchanged
    - test_openai_response_relay: Given an httpx Response with status 200 and JSON body, adapter returns AdapterResponse with same status and body bytes
    - test_openai_error_relay: Given an httpx Response with status 429, adapter returns AdapterResponse with status 429 and original error body (transparency principle)
    - test_anthropic_request_transform: Given a request body and API key, Anthropic adapter produces upstream URL "https://api.anthropic.com/v1/messages", sets "x-api-key: {key}" header, forwards body unchanged
    - test_anthropic_version_header: Anthropic adapter always includes "anthropic-version: 2023-06-01" header (adds default if client did not send one, preserves client value if sent)
    - test_anthropic_error_relay: Given an httpx Response with status 400, adapter returns AdapterResponse with status 400 and original error body
    - test_get_adapter_openai: get_adapter("/v1/chat/completions") returns OpenAIAdapter instance
    - test_get_adapter_anthropic: get_adapter("/v1/messages") returns AnthropicAdapter instance
    - test_get_adapter_unknown: get_adapter("/v1/unknown") returns None
    - test_header_stripping: Adapter strips headers matching "x-worthless-*" pattern before forwarding upstream
  </behavior>
  <action>
    RED phase: Create the adapter types and test file.

    1. Create `src/worthless/adapters/__init__.py` — empty init.

    2. Create `src/worthless/adapters/types.py` with:
       - `@dataclass(frozen=True) class AdapterRequest`: fields `url: str`, `headers: dict[str, str]`, `body: bytes`
       - `@dataclass(frozen=True) class AdapterResponse`: fields `status_code: int`, `headers: dict[str, str]`, `body: bytes`, `is_streaming: bool = False`, `stream: AsyncIterator[bytes] | None = None`
       - `class ProviderAdapter(Protocol)`: method `def prepare_request(self, *, body: bytes, headers: dict[str, str], api_key: str) -> AdapterRequest` and `async def relay_response(self, response: httpx.Response) -> AdapterResponse`

    3. Update `tests/conftest.py` — add fixtures: `sample_openai_body` (bytes of `{"model": "gpt-4", "messages": [{"role": "user", "content": "hi"}]}`), `sample_anthropic_body` (bytes of `{"model": "claude-3-5-sonnet-20241022", "max_tokens": 1024, "messages": [{"role": "user", "content": "hi"}]}`), `sample_api_key` (a fake key string).

    4. Create `tests/test_adapters.py` with all 10 tests listed in behavior. Tests import from `worthless.adapters` and use the Protocol/dataclass types. For response relay tests, create mock httpx.Response objects using `httpx.Response(status_code=..., content=..., headers=...)`. All tests should FAIL (classes not yet implemented).

    5. Run tests and confirm all fail: `uv run pytest tests/test_adapters.py -x -q` should show failures.
  </action>
  <verify>
    <automated>uv run pytest tests/test_adapters.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>10 tests exist in test_adapters.py and all fail with ImportError or NotImplementedError. Types file exports AdapterRequest, AdapterResponse, ProviderAdapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement adapters and registry to make tests pass</name>
  <files>src/worthless/adapters/openai.py, src/worthless/adapters/anthropic.py, src/worthless/adapters/registry.py, src/worthless/adapters/__init__.py</files>
  <behavior>
    All 10 tests from Task 1 pass.
  </behavior>
  <action>
    GREEN phase: Implement the adapters.

    1. Create `src/worthless/adapters/openai.py`:
       - `class OpenAIAdapter` implementing ProviderAdapter protocol
       - `UPSTREAM_URL = "https://api.openai.com/v1/chat/completions"`
       - `prepare_request`: Build AdapterRequest with upstream URL, set `Authorization: Bearer {api_key}`, copy `Content-Type` from incoming headers, strip any `x-worthless-*` headers, set body unchanged
       - `relay_response`: Read response — if not streaming, return AdapterResponse with `status_code`, response headers as dict, `response.content` as body, `is_streaming=False`

    2. Create `src/worthless/adapters/anthropic.py`:
       - `class AnthropicAdapter` implementing ProviderAdapter protocol
       - `UPSTREAM_URL = "https://api.anthropic.com/v1/messages"`
       - `prepare_request`: Build AdapterRequest with upstream URL, set `x-api-key: {api_key}`, ensure `anthropic-version` header exists (default "2023-06-01" if not in incoming headers, preserve client value if present), copy `Content-Type`, strip `x-worthless-*` headers, body unchanged
       - `relay_response`: Same pattern as OpenAI — return AdapterResponse with status, headers, body

    3. Create `src/worthless/adapters/registry.py`:
       - `_ADAPTERS: dict[str, ProviderAdapter]` mapping `"/v1/chat/completions"` to `OpenAIAdapter()` and `"/v1/messages"` to `AnthropicAdapter()`
       - `def get_adapter(path: str) -> ProviderAdapter | None`: Lookup path in registry, return adapter or None

    4. Update `src/worthless/adapters/__init__.py` — export public API: `AdapterRequest`, `AdapterResponse`, `ProviderAdapter`, `OpenAIAdapter`, `AnthropicAdapter`, `get_adapter`.

    5. Ensure httpx is in project dependencies in pyproject.toml (add if missing: `httpx>=0.28`).

    6. Run tests: `uv run pytest tests/test_adapters.py -v` — all 10 must pass.
  </action>
  <verify>
    <automated>uv run pytest tests/test_adapters.py -v --tb=short</automated>
  </verify>
  <done>All 10 adapter tests pass. OpenAI adapter sets Bearer auth and correct upstream URL. Anthropic adapter sets x-api-key and anthropic-version headers. Registry resolves paths to correct adapters. Unknown paths return None. Worthless-specific headers are stripped.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_adapters.py -v --tb=short` — all 10 tests pass
- `uv run ruff check src/worthless/adapters/` — no lint errors
- `python -c "from worthless.adapters import get_adapter, OpenAIAdapter, AnthropicAdapter; print('imports OK')"` — confirms public API
</verification>

<success_criteria>
- OpenAI adapter transforms request with correct URL and Bearer auth
- Anthropic adapter transforms request with correct URL, x-api-key, and anthropic-version
- Both adapters relay non-streaming responses with original status code and body
- Registry maps paths to correct adapters, returns None for unknown paths
- All 10 unit tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-adapters/02-01-SUMMARY.md`
</output>
