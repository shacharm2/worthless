---
phase: 02-provider-adapters
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/worthless/adapters/openai.py
  - src/worthless/adapters/anthropic.py
  - src/worthless/adapters/types.py
  - tests/test_streaming.py
  - tests/conftest.py
autonomous: true
requirements:
  - PROX-03

must_haves:
  truths:
    - "OpenAI SSE streaming response is relayed chunk-by-chunk without buffering"
    - "Anthropic SSE streaming response is relayed chunk-by-chunk without buffering"
    - "Streaming responses include correct SSE headers (Content-Type: text/event-stream, Cache-Control: no-cache, X-Accel-Buffering: no)"
    - "Streaming and non-streaming paths are selected based on the is_streaming flag on AdapterResponse"
  artifacts:
    - path: "tests/test_streaming.py"
      provides: "Integration tests for SSE streaming relay"
      min_lines: 60
    - path: "src/worthless/adapters/openai.py"
      provides: "OpenAI adapter with streaming support"
      contains: "aiter_bytes"
    - path: "src/worthless/adapters/anthropic.py"
      provides: "Anthropic adapter with streaming support"
      contains: "aiter_bytes"
  key_links:
    - from: "src/worthless/adapters/openai.py"
      to: "httpx streaming response"
      via: "async for chunk in response.aiter_bytes()"
      pattern: "aiter_bytes"
    - from: "src/worthless/adapters/anthropic.py"
      to: "httpx streaming response"
      via: "async for chunk in response.aiter_bytes()"
      pattern: "aiter_bytes"
    - from: "src/worthless/adapters/types.py"
      to: "AdapterResponse.stream"
      via: "AsyncIterator[bytes] for streaming chunks"
      pattern: "stream.*AsyncIterator"
---

<objective>
Add SSE streaming relay support to both provider adapters so chunks arrive in real-time without buffering.

Purpose: Complete the adapter layer by handling the streaming case (most real-world LLM usage is streamed). Phase 3 will wrap this in FastAPI StreamingResponse.

Output: Updated adapters with streaming detection and chunk relay, passing streaming integration tests.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-provider-adapters/02-CONTEXT.md
@.planning/phases/02-provider-adapters/02-RESEARCH.md
@.planning/phases/02-provider-adapters/02-VALIDATION.md

# Need Plan 01 output for adapter interfaces
@.planning/phases/02-provider-adapters/02-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan extends -->

From src/worthless/adapters/types.py (created in Plan 01):
```python
@dataclass(frozen=True)
class AdapterRequest:
    url: str
    headers: dict[str, str]
    body: bytes

@dataclass(frozen=True)
class AdapterResponse:
    status_code: int
    headers: dict[str, str]
    body: bytes
    is_streaming: bool = False
    stream: AsyncIterator[bytes] | None = None

class ProviderAdapter(Protocol):
    def prepare_request(self, *, body: bytes, headers: dict[str, str], api_key: str) -> AdapterRequest: ...
    async def relay_response(self, response: httpx.Response) -> AdapterResponse: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing streaming tests</name>
  <files>tests/test_streaming.py, tests/conftest.py</files>
  <behavior>
    - test_openai_sse_relay: Given a mock httpx streaming response with OpenAI SSE chunks (data: {"choices":[...]}\\n\\n lines ending with data: [DONE]\\n\\n), OpenAI adapter returns AdapterResponse with is_streaming=True and stream yields each chunk
    - test_anthropic_sse_relay: Given a mock httpx streaming response with Anthropic SSE events (event: content_block_delta\\ndata: {...}\\n\\n lines ending with event: message_stop\\ndata: {}\\n\\n), Anthropic adapter returns AdapterResponse with is_streaming=True and stream yields each chunk
    - test_streaming_headers: Both adapters set correct SSE response headers in AdapterResponse.headers: Content-Type text/event-stream, Cache-Control no-cache, X-Accel-Buffering no
    - test_no_buffering: Chunks from stream iterator are yielded individually (not accumulated into one blob). Verify by collecting chunks into a list and asserting len > 1
    - test_non_streaming_unchanged: When response Content-Type is application/json (not text/event-stream), relay_response still returns non-streaming AdapterResponse (regression guard for Plan 01 behavior)
    - test_streaming_error_passthrough: When upstream returns a non-2xx status with streaming content-type, adapter still returns the streaming response with original status code (transparency principle)
  </behavior>
  <action>
    RED phase: Create streaming test file and fixtures.

    1. Update `tests/conftest.py` — add async fixtures:
       - `mock_openai_sse_chunks`: list of bytes representing OpenAI SSE data lines: `[b'data: {"id":"chatcmpl-1","choices":[{"delta":{"content":"Hello"}}]}\\n\\n', b'data: {"id":"chatcmpl-1","choices":[{"delta":{"content":" world"}}]}\\n\\n', b'data: [DONE]\\n\\n']`
       - `mock_anthropic_sse_chunks`: list of bytes representing Anthropic SSE events: `[b'event: content_block_delta\\ndata: {"type":"content_block_delta","delta":{"type":"text_delta","text":"Hello"}}\\n\\n', b'event: content_block_delta\\ndata: {"type":"content_block_delta","delta":{"type":"text_delta","text":" world"}}\\n\\n', b'event: message_stop\\ndata: {"type":"message_stop"}\\n\\n']`
       - Helper: `make_streaming_response(chunks, status_code=200)` — creates a mock httpx.Response with `headers={"content-type": "text/event-stream"}` and an async iterator over chunks. Use `httpx.Response(status_code=status_code, headers={"content-type": "text/event-stream"}, stream=httpx.AsyncByteStream(...))` or mock the `.aiter_bytes()` method.

    2. Create `tests/test_streaming.py` with all 6 tests. Tests should call `adapter.relay_response(mock_response)` and verify the AdapterResponse has `is_streaming=True`, correct headers, and that iterating `response.stream` yields the expected chunks.

    3. Run tests: `uv run pytest tests/test_streaming.py -x -q` — all should fail (streaming not implemented yet).
  </action>
  <verify>
    <automated>uv run pytest tests/test_streaming.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>6 streaming tests exist and all fail. Mock SSE fixtures produce realistic provider chunk formats. Tests cover both providers, header correctness, no-buffering verification, and error passthrough.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement streaming relay in both adapters</name>
  <files>src/worthless/adapters/openai.py, src/worthless/adapters/anthropic.py, src/worthless/adapters/types.py</files>
  <behavior>
    All 6 streaming tests pass. All 10 existing adapter tests still pass.
  </behavior>
  <action>
    GREEN phase: Add streaming support to relay_response in both adapters.

    1. Update `relay_response` in both `OpenAIAdapter` and `AnthropicAdapter`:
       - Check response `content-type` header — if it contains "text/event-stream", handle as streaming
       - For streaming: return `AdapterResponse(status_code=response.status_code, headers={"Content-Type": "text/event-stream; charset=utf-8", "Cache-Control": "no-cache", "X-Accel-Buffering": "no", "Connection": "keep-alive"}, body=b"", is_streaming=True, stream=response.aiter_bytes())`
       - For non-streaming: existing behavior unchanged (return body bytes, is_streaming=False)
       - Do NOT parse SSE content — just forward the raw byte chunks (passthrough per CONTEXT.md)
       - Do NOT set Content-Length on streaming responses

    2. If `AdapterResponse.stream` type needs adjustment for the mock pattern, update `types.py` accordingly. The `stream` field should accept any `AsyncIterator[bytes]`.

    3. Run all tests: `uv run pytest tests/test_streaming.py tests/test_adapters.py -v --tb=short` — all 16 must pass.
  </action>
  <verify>
    <automated>uv run pytest tests/test_streaming.py tests/test_adapters.py -v --tb=short</automated>
  </verify>
  <done>All 16 tests pass (10 adapter + 6 streaming). Streaming responses set correct SSE headers. Chunks are yielded individually without buffering. Non-streaming behavior is unchanged. Error responses are passed through transparently.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_streaming.py tests/test_adapters.py -v --tb=short` — all 16 tests pass
- `uv run ruff check src/worthless/adapters/` — no lint errors
- `uv run pytest tests/ -v --tb=short` — full test suite green (including Phase 1 tests if present)
</verification>

<success_criteria>
- OpenAI SSE chunks are relayed without buffering
- Anthropic SSE chunks are relayed without buffering
- Streaming responses include correct headers (text/event-stream, no-cache, no buffering)
- Non-streaming adapter behavior is preserved (regression)
- Error streaming responses are passed through with original status code
- All 16 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-provider-adapters/02-02-SUMMARY.md`
</output>
