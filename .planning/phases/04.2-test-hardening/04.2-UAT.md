---
status: complete
phase: 04.2-test-hardening
source: [04.2-01-SUMMARY.md, 04.2-02-SUMMARY.md, 04.2-03-SUMMARY.md]
started: 2026-04-02T15:00:00Z
updated: 2026-04-02T15:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Pytest suite passes with xdist loadscope
expected: Run `uv run pytest` — all tests pass with xdist loadscope distribution and rerunfailures active
result: pass

### 2. Coverage floor script enforces per-module thresholds
expected: Run `uv run pytest --cov=worthless --cov-report=xml && python scripts/check-coverage-floors.py`. Script exits 0 if crypto >= 95%, proxy >= 85%, overall >= 80%. Exits non-zero if any threshold is missed.
result: pass

### 3. Hypothesis CI profile is reproducible
expected: Run `uv run pytest tests/test_properties.py` twice. Same results both times (derandomize=True). No flaky failures from random seed variation.
result: pass

### 4. WOR-73: CLI up/wrap tests exist and pass
expected: Run `uv run pytest tests/test_cli_up.py tests/test_cli_wrap.py -v`. See TestUpStartsProxyBackground and TestWrapSetsEnvAndRunsCommand passing.
result: pass

### 5. WOR-74: Multi-key enroll/unlock tests exist and pass
expected: Run `uv run pytest tests/test_enroll_stub.py tests/test_cli_unlock.py -v`. See TestEnrollStubMultipleKeys and TestUnlockMultipleKeys passing.
result: pass

### 6. WOR-75: Upstream error proxy tests exist and pass
expected: Run `uv run pytest tests/test_proxy.py -v -k upstream`. See 502/504/generic upstream error tests passing via respx side_effect.
result: pass

### 7. WOR-76: Hypothesis split/reconstruct property tests pass
expected: Run `uv run pytest tests/test_properties.py -v`. See TestSplitReconstructProperties with Hypothesis roundtrip tests passing.
result: pass

### 8. 5-tier CI workflows exist with correct triggers
expected: Check `.github/workflows/` contains: tests.yml (push+PR), sast.yml (push), scheduled.yml (cron), pre-release.yml (tag), benchmarks.yml (manual). Each workflow has the correct trigger and zero external secrets (only github.token).
result: pass

## Summary

total: 8
passed: 8
issues: 0
pending: 0
skipped: 0

## Gaps

[none]
