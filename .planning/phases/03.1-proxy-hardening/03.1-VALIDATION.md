---
phase: 03.1
slug: proxy-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-21
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x with pytest-asyncio, RESPX, hypothesis |
| **Config file** | pyproject.toml |
| **Quick run command** | `uv run pytest tests/test_proxy.py tests/test_proxy_hardening.py -x -q` |
| **Full suite command** | `uv run pytest --cov=worthless --cov-report=term-missing` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/test_proxy.py tests/test_proxy_hardening.py -x -q`
- **After every plan wave:** Run `uv run pytest --cov=worthless --cov-report=term-missing`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| (populated during planning) | | | CRYP-05 | integration | `uv run pytest -k "gate_before"` | ❌ W0 | ⬜ pending |
| | | | PROX-04 | integration | `uv run pytest -k "streaming"` | ❌ W0 | ⬜ pending |
| | | | PROX-05 | integration | `uv run pytest -k "error_handling"` | ❌ W0 | ⬜ pending |
| | | | SR-01 | unit | `uv run pytest -k "bytearray"` | ❌ W0 | ⬜ pending |
| | | | SR-02 | unit | `uv run pytest -k "zeroing"` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_proxy_hardening.py` — stubs for streaming, gate ordering, bytearray, error handling
- [ ] `tests/conftest.py` — update shared fixtures for split retrieve pattern
- [ ] Existing test infra covers most needs — no new framework installs

*Existing infrastructure covers framework requirements. Wave 0 adds test stubs for new behaviors.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| SSE time-to-first-token | PROX-04 | Requires real upstream or mock SSE server with timing | Start proxy with mock upstream, measure TTFT with curl |
| Memory zeroing verification | SR-02 | Python GC makes deterministic zeroing hard to assert | Inspect via debugger or memory dump after request |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
