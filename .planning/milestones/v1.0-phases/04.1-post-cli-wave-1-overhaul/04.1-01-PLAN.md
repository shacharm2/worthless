---
phase: 04.1-post-cli-wave-1-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/install-solo.md
  - src/worthless/cli/commands/wrap.py
  - src/worthless/storage/repository.py
  - pyproject.toml
  - tests/test_cli_wrap.py
autonomous: true
requirements:
  - 04.1a-BUG
  - 04.1a-LINT
  - 04.1a-PORT
  - 04.1a-TEST

must_haves:
  truths:
    - "Zero occurrences of port 8443 or 9191 in source, docs, or README"
    - "worthless wrap does not crash on a fresh database with no enrollments"
    - "uv run ruff check . returns zero errors"
    - "uv run pytest passes with zero failures"
  artifacts:
    - path: "src/worthless/cli/commands/wrap.py"
      provides: "Fixed wrap command that handles fresh DB"
    - path: "README.md"
      provides: "Port references updated to 8787"
  key_links:
    - from: "wrap.py"
      to: "repository.py"
      via: "list_enrollments query"
      pattern: "list_enrollments|decoy_hash"
---

<objective>
Fix all code prerequisites before documenting: port standardization (8787 everywhere), wrap crash on fresh DB, ruff lint errors, and test failures.

Purpose: You cannot write honest docs on top of broken code. The quickstart walkthrough in Plan 02 will fail if these bugs are not fixed first.
Output: Clean codebase — all tests pass, zero lint errors, consistent port references.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-CONTEXT.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-RESEARCH.md
@.planning/research/04.1-readme-launch/FINAL-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port standardization + wrap crash fix</name>
  <files>
    README.md,
    docs/install-solo.md,
    src/worthless/cli/commands/wrap.py,
    src/worthless/storage/repository.py
  </files>
  <action>
1. **Port standardization (8787):**
   - Run `grep -rn '8443\|9191' src/ docs/ README.md tests/ examples/ *.md` to find all occurrences.
   - Replace every occurrence of port 8443 and 9191 with 8787 in all source, docs, and config files.
   - Do NOT touch git history, .planning/ files, or research files (those are historical).

2. **Fix wrap crash on fresh DB (Beads: worthless-fit, P0):**
   - The `worthless wrap` command crashes when no keys are enrolled because `list_enrollments` returns rows missing `decoy_hash` column or the query fails on an empty/fresh database.
   - Investigate the crash: read wrap.py and trace the call to repository.py's `list_enrollments`.
   - Fix the root cause — ensure wrap handles the case where the enrollments table is empty or the `decoy_hash` column is NULL (it was added in a migration).
   - The fix must handle both: (a) fresh DB with no rows, and (b) old DB with rows that predate the decoy_hash migration.

3. **Verify:** After fixes, run `grep -rn '8443\|9191' src/ docs/ README.md` — must return zero results. Run `uv run pytest tests/test_cli_wrap.py -x` — must pass.
  </action>
  <verify>
    <automated>grep -rn '8443\|9191' src/ docs/ README.md tests/ && echo "FAIL: stale port refs" || echo "OK: no stale ports" ; uv run pytest tests/test_cli_wrap.py -x -q</automated>
  </verify>
  <done>Zero occurrences of 8443/9191 outside .planning/. Wrap command works on fresh DB without crash.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ruff lint errors + test failures</name>
  <files>
    pyproject.toml,
    src/worthless/**/*.py,
    tests/**/*.py
  </files>
  <action>
1. **Fix ruff lint errors (Beads: worthless-fc9, P1):**
   - Run `uv run ruff check . --statistics` to see error categories.
   - Fix all 93 ruff errors. Most are likely import ordering, unused imports, or formatting issues.
   - Run `uv run ruff check .` — must return zero errors.

2. **Fix test failures (Beads: worthless-c0q, P1):**
   - Run `uv run pytest` to identify which 8 tests fail and why.
   - The scipy dependency issue: if tests import scipy and it is not in dependencies, either add it to test dependencies or remove the scipy-dependent test (the chi-squared decoy test may need scipy).
   - Fix each failing test. If a test depends on scipy for statistical validation of decoy indistinguishability, add `scipy` to the `[project.optional-dependencies.test]` group in pyproject.toml.
   - Run `uv run pytest` — all tests must pass.

3. **Final verification:** Run `uv run pytest && uv run ruff check .` — both must succeed with zero failures/errors.
  </action>
  <verify>
    <automated>cd /Users/shachar/Projects/worthless/worthless && uv run pytest -x -q && uv run ruff check .</automated>
  </verify>
  <done>All tests pass. Zero ruff errors. Codebase is clean and ready for documentation.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `uv run pytest` — all tests green
2. `uv run ruff check .` — zero errors
3. `grep -rn '8443\|9191' src/ docs/ README.md tests/` — zero results
4. `uv run python -m worthless.cli.main wrap --help` — no crash
</verification>

<success_criteria>
- Zero port references to 8443 or 9191 in source/docs
- wrap command handles fresh DB gracefully
- All pytest tests pass
- Zero ruff lint errors
- Codebase ready for documentation phase
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-01-SUMMARY.md`
</output>
