---
phase: 04.1-post-cli-wave-1-overhaul
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worthless/cli/commands/scan.py
  - tests/test_cli_scan.py
  - README.md
autonomous: true
gap_closure: true
requirements:
  - 04.1a-TEST
  - 04.1b-README
  - 04.1b-DOCS
  - 04.1a-BUG
  - 04.1a-LINT
  - 04.1a-PORT
  - 04.1b-EXAMPLES
  - 04.1b-PRECOMMIT
  - 04.1b-WALKTHROUGH
  - 04.1c-RENAME

must_haves:
  truths:
    - "uv run pytest passes with zero failures including test_deep_scan_cleans_up_temp_file"
    - "README CLI reference table uses lock/unlock terminology only, zero occurrences of enroll"
    - "README contains a markdown link to docs/PROTOCOL.md"
  artifacts:
    - path: "src/worthless/cli/commands/scan.py"
      provides: "Deep scan temp file cleanup fix"
      contains: "finally"
    - path: "README.md"
      provides: "Fixed CLI reference table and PROTOCOL.md link"
  key_links:
    - from: "README.md"
      to: "docs/PROTOCOL.md"
      via: "markdown link"
      pattern: "PROTOCOL\\.md"
---

<objective>
Close 3 verification gaps from phase 04.1: fix scan --deep temp file leak causing test failure, fix README enroll terminology in CLI reference table, and add missing README link to docs/PROTOCOL.md.

Purpose: All 3 gaps block full phase verification. The test failure blocks 04.1a-TEST, the enroll terminology violates 04.1b-README locked decision, and the missing link breaks a key_link from Plan 02.
Output: All 3 gaps resolved, full test suite green, verification score 12/12.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix scan --deep temp file leak</name>
  <files>src/worthless/cli/commands/scan.py, tests/test_cli_scan.py</files>
  <action>
Diagnose why test_deep_scan_cleans_up_temp_file fails despite the existing finally block at line 316-318 of scan.py.

The scan command's main function has a try/finally that should clean up tmp_file. Possible causes:
1. The test doesn't strip API keys from env (unlike test_deep_scan_clean_dir_exits_0 at line 323-326), so scan_files() may raise an unexpected exception that somehow bypasses cleanup
2. An early return or exit path before the try block but after _collect_deep_paths
3. The runner.invoke() test harness intercepting the exception before finally runs

Steps:
- Run `uv run pytest tests/test_cli_scan.py::TestScanDeep::test_deep_scan_cleans_up_temp_file -xvs` to see the actual failure output
- Inspect whether the test creates an environment where the scan command exits before reaching the try block (e.g., the format validation at line 257 exits before tmp_file assignment)
- Fix the root cause. If the finally block is correct but the test environment leaks API keys that cause a different code path, the fix might be in the test (strip env keys like the adjacent test does) OR in the scan command (move cleanup to a broader scope)
- Ensure the fix is minimal — do not restructure working code unnecessarily

After fixing, verify: `uv run pytest tests/test_cli_scan.py::TestScanDeep -xvs` — all deep scan tests pass including test_deep_scan_cleans_up_temp_file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli_scan.py::TestScanDeep::test_deep_scan_cleans_up_temp_file -x</automated>
  </verify>
  <done>test_deep_scan_cleans_up_temp_file passes. No worthless-env-* temp files leaked after scan --deep.</done>
</task>

<task type="auto">
  <name>Task 2: Fix README enroll terminology and add PROTOCOL.md link</name>
  <files>README.md</files>
  <action>
Two fixes in README.md:

1. **Fix enroll terminology in CLI reference table (lines 96-98):**
   - Line 96: Change `worthless status | Show enrolled keys and proxy health` to `worthless status | Show locked keys and proxy health`
   - Line 98: Either remove the `worthless enroll` row entirely (it's a scripting primitive, not a user-facing command per CONTEXT.md) OR rename it to something like `worthless enroll | Lock a single key (scripting/CI)` — but per locked decision "never enroll in user-facing docs", the safest approach is to REMOVE the row. The lock command is the user-facing equivalent.
   - After editing, verify: `grep -in 'enroll' README.md` returns zero results

2. **Add link to docs/PROTOCOL.md:**
   - In the Security section (around line 111-115), add a reference to the wire protocol doc. For example, after the SECURITY_RULES.md reference, add: `For wire protocol details (headers, endpoints, error codes), see [docs/PROTOCOL.md](docs/PROTOCOL.md).`
   - Alternatively, add it to the CLI reference section as a "Learn more" link
   - Verify: `grep 'PROTOCOL.md' README.md` returns at least one result
  </action>
  <verify>
    <automated>grep -c 'enroll' README.md | grep -q '^0$' && grep -q 'PROTOCOL.md' README.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README has zero occurrences of "enroll" and contains at least one link to docs/PROTOCOL.md.</done>
</task>

</tasks>

<verification>
- `uv run pytest -x` — full test suite passes with zero failures
- `uv run ruff check .` — zero lint errors
- `grep -in 'enroll' README.md` — zero results
- `grep 'PROTOCOL.md' README.md` — at least one result
- No worthless-env-* files leaked in /tmp after test run
</verification>

<success_criteria>
- All 230 tests pass including test_deep_scan_cleans_up_temp_file
- README CLI reference table uses lock/unlock only
- README links to docs/PROTOCOL.md
- Verification score reaches 12/12 (was 9/12)
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-04-SUMMARY.md`
</output>
