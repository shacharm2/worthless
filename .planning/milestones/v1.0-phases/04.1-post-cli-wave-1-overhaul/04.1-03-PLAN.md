---
phase: 04.1-post-cli-wave-1-overhaul
plan: 03
type: execute
wave: 3
depends_on: ["04.1-02"]
files_modified:
  - src/worthless/proxy/app.py
  - src/worthless/cli/commands/lock.py
  - src/worthless/cli/commands/wrap.py
  - src/worthless/cli/commands/status.py
  - src/worthless/cli/commands/up.py
  - src/worthless/storage/repository.py
  - tests/test_proxy.py
  - tests/test_proxy_hardening.py
  - tests/test_cli_wrap.py
  - docs/PROTOCOL.md
  - README.md
autonomous: true
requirements:
  - 04.1c-RENAME

must_haves:
  truths:
    - "Zero occurrences of x-worthless-alias in any source or test file"
    - "All proxy tests pass after rename"
    - "x-worthless-key is used consistently in source, tests, and docs"
  artifacts:
    - path: "src/worthless/proxy/app.py"
      provides: "Proxy using x-worthless-key header"
    - path: "tests/test_proxy.py"
      provides: "Proxy tests using x-worthless-key"
    - path: "tests/test_proxy_hardening.py"
      provides: "Hardening tests using x-worthless-key"
  key_links:
    - from: "src/worthless/proxy/app.py"
      to: "tests/test_proxy.py"
      via: "header name constant"
      pattern: "x-worthless-key"
    - from: "src/worthless/cli/commands/lock.py"
      to: "src/worthless/proxy/app.py"
      via: "header sent by CLI, read by proxy"
      pattern: "x-worthless-key"
---

<objective>
Rename the wire protocol header from `x-worthless-alias` to `x-worthless-key` across all source code, tests, and documentation. One atomic commit on a separate branch.

Purpose: "key" parallels `x-api-key`, is immediately clear to developers and agents. Pre-release with zero users means zero breaking change cost. This must be a separate branch/PR to keep the mechanical rename independently reviewable and avoid merge conflicts with the docs rewrite.
Output: All occurrences of x-worthless-alias replaced with x-worthless-key. Full test suite passes.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-CONTEXT.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-RESEARCH.md
@.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename x-worthless-alias to x-worthless-key</name>
  <files>
    src/worthless/proxy/app.py,
    src/worthless/cli/commands/lock.py,
    src/worthless/cli/commands/wrap.py,
    src/worthless/cli/commands/status.py,
    src/worthless/cli/commands/up.py,
    src/worthless/storage/repository.py,
    tests/test_proxy.py,
    tests/test_proxy_hardening.py,
    tests/test_cli_wrap.py,
    docs/PROTOCOL.md,
    README.md
  </files>
  <action>
**IMPORTANT:** This task runs on a **separate git branch** from the docs work. Create branch `gsd/04.1c-header-rename` from the current branch (which should have Plan 01 and 02 merged).

1. **Find all occurrences:**
   Run `grep -rn 'x-worthless-alias' src/ tests/ docs/ README.md examples/` to get the full list.
   Research showed ~59 occurrences across ~12 files. Verify the actual count.

2. **Rename in source code:**
   - `src/worthless/proxy/app.py` — header extraction, request routing (~3 occurrences)
   - Any CLI commands that reference the header name (lock.py, wrap.py, status.py, up.py)
   - `src/worthless/storage/repository.py` — if it references the header name
   - Look for any constants or config that define the header name — rename those first, then dependents

3. **Rename in tests:**
   - `tests/test_proxy.py` (~16 occurrences) — test fixtures, assertions, header construction
   - `tests/test_proxy_hardening.py` (~25 occurrences) — same
   - `tests/test_cli_wrap.py` — if it references the header
   - Any other test files found by grep

4. **Rename in docs (catch any the README rewrite missed):**
   - `docs/PROTOCOL.md` — should already use x-worthless-key from Plan 02, but verify
   - `README.md` — should already use x-worthless-key from Plan 02, but verify
   - Any other docs/ files

5. **Verify zero remaining occurrences:**
   `grep -rn 'x-worthless-alias' src/ tests/ docs/ README.md examples/` must return zero results.

6. **Run full test suite:**
   `uv run pytest` — all tests must pass with the new header name.
   `uv run ruff check .` — zero lint errors.

7. **Commit as one atomic commit** on the separate branch. Commit message: `refactor: rename x-worthless-alias header to x-worthless-key`
  </action>
  <verify>
    <automated>grep -rn 'x-worthless-alias' src/ tests/ docs/ README.md examples/ && echo "FAIL: alias refs remain" || echo "OK: no alias refs" ; uv run pytest -x -q && uv run ruff check .</automated>
  </verify>
  <done>Zero occurrences of x-worthless-alias in source, tests, or docs. All tests pass. Single atomic commit on separate branch ready for PR.</done>
</task>

</tasks>

<verification>
After task completes:
1. `grep -rn 'x-worthless-alias' src/ tests/ docs/ README.md examples/` — zero results
2. `grep -rn 'x-worthless-key' src/ tests/` — confirms new name is present
3. `uv run pytest` — all tests pass
4. `uv run ruff check .` — zero errors
5. `git log --oneline -1` — shows atomic rename commit
</verification>

<success_criteria>
- Zero occurrences of x-worthless-alias anywhere in the working tree (excluding .planning/ and git history)
- x-worthless-key used consistently everywhere
- Full test suite passes
- Single atomic commit on separate branch
- Ready for independent PR review
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-post-cli-wave-1-overhaul/04.1-03-SUMMARY.md`
</output>
