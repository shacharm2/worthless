---
phase: 04.2-test-hardening
plan: 02
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified:
  - tests/test_cli_up.py
  - tests/test_cli_wrap.py
  - tests/test_enroll_stub.py
  - tests/test_cli_unlock.py
  - tests/test_proxy.py
  - tests/test_properties.py
  - tests/test_decoy.py
  - tests/conftest.py
autonomous: true
requirements:
  - WOR-73
  - WOR-74
  - WOR-75
  - WOR-76

must_haves:
  truths:
    - "CLI up command is tested via CliRunner (WOR-73)"
    - "CLI wrap command is tested via CliRunner (WOR-73)"
    - "enroll_stub handles multi-key enrollment (WOR-74)"
    - "unlock handles multi-key scenarios (WOR-74)"
    - "Proxy returns 502 when upstream is unreachable (WOR-75)"
    - "Proxy returns 504 when upstream times out (WOR-75)"
    - "split/reconstruct roundtrip holds for arbitrary key bytes (WOR-76)"
    - "Flaky decoy test is fixed or quarantined (04.1 carryover)"
    - "xdist tempdir isolation has no latent races (04.1 carryover)"
  artifacts:
    - path: "tests/test_cli_up.py"
      provides: "CLI up CliRunner tests"
    - path: "tests/test_cli_wrap.py"
      provides: "CLI wrap CliRunner tests"
    - path: "tests/test_proxy.py"
      provides: "502/504 upstream error tests"
      contains: "502"
    - path: "tests/test_properties.py"
      provides: "Hypothesis roundtrip property tests"
      contains: "given"
  key_links:
    - from: "tests/test_proxy.py"
      to: "src/worthless/proxy"
      via: "respx mock returning 502/504"
      pattern: "respx.*502|504"
    - from: "tests/test_properties.py"
      to: "src/worthless/crypto/splitter.py"
      via: "Hypothesis strategy for arbitrary bytes"
      pattern: "st\\.binary|split.*reconstruct"
---

<objective>
Close the WOR-67 test backlog by writing missing tests (WOR-73, WOR-74, WOR-75, WOR-76) and fixing 04.1 carryover items (flaky decoy test, xdist tempdir races, mutmut:skip audit).

Purpose: Close known coverage gaps and stabilize flaky tests before CI wiring.
Output: Extended test files covering CLI up/wrap, multi-key enroll/unlock, proxy 502/504, roundtrip properties, and fixed flaky tests.
</objective>

<execution_context>
@/Users/shachar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shachar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-test-hardening/04.2-CONTEXT.md
@.planning/phases/04.2-test-hardening/04.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WOR-73 + WOR-74 + WOR-75 + WOR-76 test backlog</name>
  <files>tests/test_cli_up.py, tests/test_cli_wrap.py, tests/test_enroll_stub.py, tests/test_cli_unlock.py, tests/test_proxy.py, tests/test_properties.py</files>
  <behavior>
    - WOR-73: test_up_starts_proxy_background (CliRunner invokes `up`, mocked subprocess confirms proxy launch)
    - WOR-73: test_wrap_sets_env_and_runs_command (CliRunner invokes `wrap`, confirms env vars set and subprocess called)
    - WOR-74: test_enroll_stub_multiple_keys (enroll two different keys, both stored, both retrievable)
    - WOR-74: test_unlock_multiple_keys (unlock with multiple enrolled keys, each reconstructs correctly)
    - WOR-75: test_proxy_upstream_502 (respx returns 502, proxy returns sanitized 502 to client)
    - WOR-75: test_proxy_upstream_504_timeout (respx returns 504, proxy returns sanitized 504 to client)
    - WOR-76: test_split_reconstruct_roundtrip_property (Hypothesis: for arbitrary bytes 16-128 len, split then reconstruct equals original)
    - WOR-76: test_split_reconstruct_empty_key_rejected (zero-length key raises ValueError)
  </behavior>
  <action>
For each WOR item, extend the existing test file (do NOT create new files):

**WOR-73 (tests/test_cli_up.py, tests/test_cli_wrap.py):**
- Read existing tests first to understand current coverage
- Add CliRunner-based tests that invoke the CLI commands
- Mock subprocess/process creation for `up` command
- Mock environment variable setting for `wrap` command
- Follow test naming: `test_<what>_<condition>_<expected>`

**WOR-74 (tests/test_enroll_stub.py, tests/test_cli_unlock.py):**
- Add multi-key scenario tests
- Test that enrolling key A then key B results in both being stored
- Test that unlock can handle multiple enrolled aliases

**WOR-75 (tests/test_proxy.py):**
- Add tests using respx to mock upstream returning 502 and 504
- Verify proxy sanitizes the error response (per Phase 03.1 decision: keep status code, replace message)
- Use existing proxy test patterns (ASGITransport, httpx.AsyncClient)

**WOR-76 (tests/test_properties.py):**
- Extend existing Hypothesis property tests
- Add roundtrip property: `given(st.binary(min_size=16, max_size=128))` -> split -> reconstruct == original
- Add edge case: empty/zero-length key rejection
- Use `@settings(max_examples=200)` locally, CI profile caps at 50

Run tests after each file change:
```bash
uv run pytest tests/test_cli_up.py tests/test_cli_wrap.py tests/test_enroll_stub.py tests/test_cli_unlock.py tests/test_proxy.py tests/test_properties.py -x --no-header -q
```
  </action>
  <verify>
    <automated>cd /Users/shachar/Projects/worthless/worthless && uv run pytest tests/test_cli_up.py tests/test_cli_wrap.py tests/test_enroll_stub.py tests/test_cli_unlock.py tests/test_proxy.py tests/test_properties.py -x --no-header -q</automated>
  </verify>
  <done>All WOR-73/74/75/76 tests pass. CLI up/wrap tested via CliRunner, multi-key enroll/unlock tested, proxy 502/504 tested, roundtrip property tested with Hypothesis.</done>
</task>

<task type="auto">
  <name>Task 2: Fix carryover items (flaky decoy, tempdir audit, mutmut:skip)</name>
  <files>tests/test_decoy.py, tests/conftest.py</files>
  <action>
1. **Flaky decoy test (04.1 carryover #1):**
   - Read tests/test_decoy.py to identify the flaky test (noted in 04.1)
   - Determine root cause: likely statistical assertion with tight bounds or timing dependency
   - Fix the test to be deterministic. If the test is inherently statistical, widen the bounds or use a fixed seed.
   - If unfixable, mark with `@pytest.mark.quarantine` and file a beads issue with `bd create "Quarantined flaky decoy test: [reason]"`

2. **xdist tempdir isolation audit (04.1 carryover #2):**
   - Search all test files for `tmp_path`, `tempfile`, `NamedTemporaryFile`, `mkdtemp` usage:
     ```bash
     grep -rn "tmp_path\|tempfile\|mkdtemp\|TemporaryFile\|TemporaryDirectory" tests/
     ```
   - For each hit, verify the temp path is unique per test (not sharing a hardcoded path)
   - Fix any tests that use shared temp paths -- each test must create its own isolated temp directory
   - Phase 04.1-04 fixed deep_scan; check for similar patterns in other test files

3. **mutmut:skip audit (04.1 carryover #3):**
   - Check splitter.py line 44 for the mutmut:skip comment:
     ```bash
     sed -n '40,50p' src/worthless/crypto/splitter.py
     ```
   - Run mutmut on the crypto module to check for survivors:
     ```bash
     uv run python -m mutmut run --paths-to-mutate src/worthless/crypto/splitter.py --runner "uv run pytest tests/test_splitter.py -x --no-header -q" 2>&1 | tail -20
     ```
   - If there are surviving mutants beyond the skip, add targeted tests to kill them
   - Document any intentional survivors in tests/mutmut_allowlist.md

4. Run full suite with xdist to confirm isolation:
   ```bash
   uv run pytest -n auto --dist loadscope -x --no-header -q
   ```
  </action>
  <verify>
    <automated>cd /Users/shachar/Projects/worthless/worthless && uv run pytest -n auto --dist loadscope -x --no-header -q && uv run pytest tests/test_decoy.py -x --no-header -q</automated>
  </verify>
  <done>Flaky decoy test fixed or quarantined, all tempdir usages are per-test isolated, mutmut:skip at splitter.py:44 audited with no unexpected survivors</done>
</task>

</tasks>

<verification>
- `uv run pytest -x --no-header -q` full suite passes
- `uv run pytest -n auto --dist loadscope -x` passes with xdist loadscope
- WOR-73 tests: `uv run pytest tests/test_cli_up.py tests/test_cli_wrap.py -v -k "up or wrap"` shows new tests
- WOR-75 tests: `uv run pytest tests/test_proxy.py -v -k "502 or 504"` shows upstream error tests
- WOR-76 tests: `uv run pytest tests/test_properties.py -v -k "roundtrip"` shows property tests
- Decoy test: `uv run pytest tests/test_decoy.py -x` passes reliably (run 3 times)
</verification>

<success_criteria>
- WOR-73: CLI up and wrap have CliRunner tests
- WOR-74: Multi-key enroll/unlock scenarios tested
- WOR-75: Proxy 502/504 upstream errors tested with respx
- WOR-76: Hypothesis roundtrip property test for split/reconstruct
- Flaky decoy test either fixed deterministically or quarantined with beads issue
- No xdist tempdir races in any test file
- mutmut:skip at splitter.py:44 audited
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-test-hardening/04.2-02-SUMMARY.md`
</output>
