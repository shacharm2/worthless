# Staged copy of the canonical install.sh (kept at the repo root). Both
# vitest and wrangler require the file inside this Worker's project
# directory; the `stage:install-sh` npm script copies it in before any
# build step. The copy is generated, not source — never commit.
install.sh

# Wrangler deploy artifacts
.wrangler/

# Node
node_modules/
