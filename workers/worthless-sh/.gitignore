# Staged copy of the canonical install.sh (no longer used — kept here
# in case anyone manually runs `cp ../../install.sh ./install.sh`).
/install.sh

# Auto-generated by `scripts/embed-assets.mjs`. Contains base64-encoded
# install.sh + walkthrough.txt that the Worker decodes at module load.
# Regenerated automatically as `pretest` / `predev` / `predeploy`; never
# commit.
/src/embedded.ts

# Wrangler deploy artifacts
.wrangler/

# Node
node_modules/
