---
title: "Make API keys worthless to steal"
description: "Split-key proxy with hard spend caps. Your leaked .env becomes worthless."
template: splash
hero:
  title: Make API keys worthless to steal.
  tagline: Split-key proxy with hard spend caps. Leaked .env? Useless.
  actions:
    - text: Install in 90 seconds
      link: /install-solo/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/shacharm2/worthless
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Pick how you install

<CardGrid>
  <Card title="Solo dev" icon="laptop">
    `pipx install worthless`. Done in 90 seconds.

    [Get started →](/install-solo/)
  </Card>
  <Card title="Docker" icon="seti:docker">
    Signed multi-arch image from GHCR. No build.

    [Pull image →](/install-docker/)
  </Card>
  <Card title="Claude Code, Cursor, Windsurf" icon="seti:code-search">
    MCP server. Point your editor's SDK at the proxy.

    [Wire it up →](/install-mcp/)
  </Card>
  <Card title="GitHub Actions" icon="github">
    Protect keys during CI runs. Scan for leaks.

    [Add to CI →](/install-github-actions/)
  </Card>
</CardGrid>

## How it works

Three invariants. No exceptions.

- **Client-side splitting.** Your key is split on your machine. The server only ever sees half.
- **Gate before reconstruction.** Budget exceeded? The key never forms. Zero provider calls.
- **Direct upstream.** The reconstructed key calls the LLM provider directly. It never returns to the proxy.

Read the [security model](/security/) and [wire protocol](/protocol/) for the full picture.
